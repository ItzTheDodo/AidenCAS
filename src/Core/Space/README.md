# Space subsystem

`TopologicalSpace` is a minimal wrapper around a set of elements.

## Current API

- `TopologicalSpace(elements)`
- `elements` property

This module is currently a structural placeholder for future space/topology features.

